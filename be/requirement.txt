# Web framework
fastapi==0.115.2
uvicorn[standard]==0.30.1

# Pydantic v2
pydantic==2.8.2
pydantic-settings==2.3.4

# LLM client (OpenAI)
openai==1.37.0

# Speech-to-Text / TTS (tùy chọn)
aiofiles==23.2.1

# RAG vector stores (chọn cái bạn dùng)
chromadb==0.4.24
qdrant-client==1.9.0

# Token Auth (JWT)
python-jose==3.3.0
passlib[bcrypt]==1.7.4

# Redis (nếu dùng state store cho story progress)
redis==5.0.4

# Utilities
python-dotenv==1.0.1
httpx==0.27.2
requests==2.32.3

# Optional NLP helpers
rapidfuzz==3.7.0
